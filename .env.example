# Hub login (required to open `/`). Copy to `.env` and set strong values.
# Note: Vite inlines these into the client bundle — use real backend auth for production secrets.
VITE_HUB_USERNAME=admin
VITE_HUB_PASSWORD=admin1234
